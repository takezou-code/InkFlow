diff --git a/app/src/main/java/com/vic/inkflow/ui/InkLayerApp.kt b/app/src/main/java/com/vic/inkflow/ui/InkLayerApp.kt
index 514a902..a7264ac 100644
--- a/app/src/main/java/com/vic/inkflow/ui/InkLayerApp.kt
+++ b/app/src/main/java/com/vic/inkflow/ui/InkLayerApp.kt
@@ -1,4 +1,4 @@
-package com.vic.inkflow.ui
+Ôªøpackage com.vic.inkflow.ui
 
 import android.content.Intent
 import android.graphics.Bitmap
@@ -50,6 +50,7 @@ import androidx.compose.foundation.interaction.collectIsPressedAsState
 import androidx.compose.foundation.layout.Arrangement
 import androidx.compose.foundation.layout.Box
 import androidx.compose.foundation.layout.BoxScope
+import androidx.compose.foundation.layout.BoxWithConstraints
 import androidx.compose.foundation.layout.Column
 import androidx.compose.foundation.layout.PaddingValues
 import androidx.compose.foundation.layout.Row
@@ -252,12 +253,12 @@ fun InkLayerApp(db: AppDatabase) {
  * Full-screen flowing gradient that always covers the entire component.
  *
  * Instead of moving start/end coordinates (which leaves gaps), this keeps
- * the gradient anchored at Offset.Zero ??Offset.Infinite (auto-expands to
+ * the gradient anchored at Offset.Zero ??Offset.Infinite (auto-expands to
  * actual layout bounds) and cycles the *colour values* themselves through
  * the palette using linear interpolation.
  *
- * Dark mode  ??very dark navy/indigo/purple.
- * Light mode ??soft lavender/sky/violet pastels.
+ * Dark mode  ??very dark navy/indigo/purple.
+ * Light mode ??soft lavender/sky/violet pastels.
  */
 @Composable
 private fun rememberFlowingBrandBrush(isDarkTheme: Boolean): Brush {
@@ -277,7 +278,7 @@ private fun rememberFlowingBrandBrush(isDarkTheme: Boolean): Brush {
     val n = palette.size
 
     val transition = rememberInfiniteTransition(label = "BrandGradientFlow")
-    // offset travels 0 ??n over 7 s, then wraps ??driving the colour cycle
+    // offset travels 0 ??n over 7 s, then wraps ??driving the colour cycle
     val offset by transition.animateFloat(
         initialValue = 0f,
         targetValue = n.toFloat(),
@@ -296,7 +297,7 @@ private fun rememberFlowingBrandBrush(isDarkTheme: Boolean): Brush {
         lerp(palette[lo], palette[hi], pos - pos.toInt())
     }
 
-    // Offset.Zero ??Offset.Infinite: Compose expands to the full component size,
+    // Offset.Zero ??Offset.Infinite: Compose expands to the full component size,
     // so the gradient always fills corner-to-corner regardless of screen dimensions.
     return Brush.linearGradient(
         colors = animatedColors,
@@ -351,13 +352,13 @@ fun DocumentLibraryScreen(
                     val newUri = PdfManager.createBlankPdf(context, widthPt, heightPt)
                     if (newUri != null) {
                         kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.Main) {
-                            docViewModel.recordOpened(newUri.toString(), "?™ÂëΩ?çÁ?Ë®?)
+                            docViewModel.recordOpened(newUri.toString(), "?ÔøΩÂëΩ?ÔøΩÔøΩ?Ôø?)
                             val encodedUri = URLEncoder.encode(newUri.toString(), StandardCharsets.UTF_8.toString())
                             navController.navigate("editor/$encodedUri")
                         }
                     } else {
                         kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.Main) {
-                            android.widget.Toast.makeText(context, "?°Ê?Âª∫Á?Á©∫ÁôΩÁ≠ÜË?", android.widget.Toast.LENGTH_SHORT).show()
+                            android.widget.Toast.makeText(context, "?ÔøΩÔøΩ?Âª∫ÔøΩ?Á©∫ÁôΩÁ≠ÜÔøΩ?", android.widget.Toast.LENGTH_SHORT).show()
                         }
                     }
                 }
@@ -389,7 +390,7 @@ fun DocumentLibraryScreen(
                         }
                     } else {
                         kotlinx.coroutines.withContext(kotlinx.coroutines.Dispatchers.Main) {
-                            android.widget.Toast.makeText(context, "?°Ê?Ë§áË£Ω PDFÔºåË??çË©¶", android.widget.Toast.LENGTH_SHORT).show()
+                            android.widget.Toast.makeText(context, "?ÔøΩÔøΩ?Ë§áË£Ω PDFÔºåÔøΩ??ÔøΩË©¶", android.widget.Toast.LENGTH_SHORT).show()
                         }
                     }
                 }
@@ -397,7 +398,7 @@ fun DocumentLibraryScreen(
         }
     )
 
-    // Vivid brand gradient for the FAB / logo (never animated ??static)
+    // Vivid brand gradient for the FAB / logo (never animated ??static)
     val brandGradient = remember {
         androidx.compose.ui.graphics.Brush.linearGradient(
             listOf(androidx.compose.ui.graphics.Color(0xFF6366F1), androidx.compose.ui.graphics.Color(0xFFA855F7))
@@ -441,14 +442,14 @@ fun DocumentLibraryScreen(
             .fillMaxSize()
             .statusBarsPadding()
     ) {
-        // Navigation Rail ??transparent so the full-screen gradient shows through
+        // Navigation Rail ??transparent so the full-screen gradient shows through
         androidx.compose.material3.NavigationRail(
             modifier = Modifier.fillMaxHeight(),
             containerColor = Color.Transparent,
             contentColor = MaterialTheme.colorScheme.onBackground
         ) {
             Spacer(Modifier.height(8.dp))
-            // Brand logo ??vivid flowing gradient circle
+            // Brand logo ??vivid flowing gradient circle
             Box(
                 modifier = Modifier
                     .size(40.dp)
@@ -465,32 +466,32 @@ fun DocumentLibraryScreen(
             NavigationRailItem(
                 selected = selectedNavIndex == 0,
                 onClick = { selectedNavIndex = 0 },
-                icon = { Icon(Icons.Default.Home, contentDescription = "È¶ñÈ?") },
-                label = { Text("È¶ñÈ?") }
+                icon = { Icon(Icons.Default.Home, contentDescription = "È¶ñÔøΩ?") },
+                label = { Text("È¶ñÔøΩ?") }
             )
             NavigationRailItem(
                 selected = selectedNavIndex == 1,
                 onClick = { selectedNavIndex = 1 },
-                icon = { Icon(Icons.Default.Folder, contentDescription = "Ë≥áÊ?Â§?) },
-                label = { Text("Ë≥áÊ?Â§?) }
+                icon = { Icon(Icons.Default.Folder, contentDescription = "Ë≥áÔøΩ?Ôø?) },
+                label = { Text("Ë≥áÔøΩ?Ôø?) }
             )
             NavigationRailItem(
                 selected = selectedNavIndex == 2,
                 onClick = { selectedNavIndex = 2 },
-                icon = { Icon(Icons.Default.Bookmark, contentDescription = "?∂Ë?") },
-                label = { Text("?∂Ë?") }
+                icon = { Icon(Icons.Default.Bookmark, contentDescription = "?ÔøΩÔøΩ?") },
+                label = { Text("?ÔøΩÔøΩ?") }
             )
             Spacer(Modifier.weight(1f))
             // Dark / Light mode toggle
             IconButton(onClick = onToggleDarkTheme) {
                 Icon(
                     imageVector = if (isDarkTheme) Icons.Outlined.LightMode else Icons.Outlined.DarkMode,
-                    contentDescription = "?áÊ?‰∏ªÈ?"
+                    contentDescription = "?ÔøΩÔøΩ?‰∏ªÔøΩ?"
                 )
             }
             // Settings
             IconButton(onClick = { /* TODO: Settings */ }) {
-                Icon(Icons.Default.Settings, contentDescription = "Ë®≠Â?")
+                Icon(Icons.Default.Settings, contentDescription = "Ë®≠ÔøΩ?")
             }
             Spacer(Modifier.height(8.dp))
         }
@@ -553,18 +554,18 @@ fun DocumentLibraryScreen(
                         ) {
                             Column(verticalArrangement = Arrangement.spacedBy(2.dp)) {
                                 Text(
-                                    text = "?úÂ?ÁµêÊ?",
+                                    text = "?ÔøΩÔøΩ?ÁµêÔøΩ?",
                                     style = MaterialTheme.typography.labelLarge,
                                     color = MaterialTheme.colorScheme.primary
                                 )
                                 Text(
-                                    text = "\"$normalizedQuery\" Â∞çÊ? ${filteredDocs.size} ‰ªΩÁ?Ë®?,
+                    text = "Please select", 
                                     style = MaterialTheme.typography.bodySmall,
                                     color = MaterialTheme.colorScheme.onSurfaceVariant
                                 )
                             }
                             TextButton(onClick = { searchQuery = "" }) {
-                                Text("Ê∏ÖÈô§?úÂ?")
+                                Text("Ê∏ÖÈô§?ÔøΩÔøΩ?")
                             }
                         }
                     }
@@ -674,14 +675,14 @@ private fun LibraryHeroPanel(
                             color = MaterialTheme.colorScheme.primary
                         )
                         Text(
-                            text = "?ä‰?Â§©Á? PDF?ÅË??ñË?Ë®ªË??Ü‰∏≠?®Â?‰∏Ä?ãÂ∑•‰ΩúÂè∞",
+                            text = "?ÔøΩÔøΩ?Â§©ÔøΩ? PDF?ÔøΩÔøΩ??ÔøΩÔøΩ?Ë®ªÔøΩ??ÔøΩ‰∏≠?ÔøΩÔøΩ?‰∏Ä?ÔøΩÂ∑•‰ΩúÂè∞",
                             style = MaterialTheme.typography.headlineSmall
                         )
                         Text(
                             text = if (searchQuery.isBlank()) {
-                                "È¶ñÈ??æÂú®?¥Â??µ‰?Ê°åÈù¢ÔºöÊ?Â∞ã„ÄÅÈ?Ê™îË??∞Âª∫?•Âè£?ΩÈ?‰∏≠Âú®?ôË£°??
+                                "È¶ñÔøΩ??ÔøΩÂú®?ÔøΩÔøΩ??ÔøΩÔøΩ?Ê°åÈù¢ÔºöÔøΩ?Â∞ã„ÄÅÔøΩ?Ê™îÔøΩ??ÔøΩÂª∫?ÔøΩÂè£?ÔøΩÔøΩ?‰∏≠Âú®?ÔøΩË£°??
                             } else {
-                                "‰Ω†Ê≠£?®Ê™¢Ë¶?\"${searchQuery.trim()}\" ?ÑÊ?Â∞ãÁ??ú„Ä?
+                                "‰Ω†Ê≠£?ÔøΩÊ™¢Ôø?\"${searchQuery.trim()}\" ?ÔøΩÔøΩ?Â∞ãÔøΩ??ÔøΩÔøΩ?
                             },
                             style = MaterialTheme.typography.bodyMedium,
                             color = MaterialTheme.colorScheme.onSurfaceVariant
@@ -697,7 +698,7 @@ private fun LibraryHeroPanel(
                             horizontalAlignment = Alignment.CenterHorizontally
                         ) {
                             Text(totalDocuments.toString(), style = MaterialTheme.typography.titleLarge)
-                            Text("Á≠ÜË?Â∫?, style = MaterialTheme.typography.labelMedium)
+                            Text("Á≠ÜÔøΩ?Ôø?, style = MaterialTheme.typography.labelMedium)
                         }
                     }
                 }
@@ -712,11 +713,11 @@ private fun LibraryHeroPanel(
                     trailingIcon = {
                         AnimatedVisibility(visible = searchQuery.isNotBlank()) {
                             IconButton(onClick = { onSearchQueryChange("") }) {
-                                Icon(Icons.Default.Close, contentDescription = "Ê∏ÖÈô§?úÂ?")
+                                Icon(Icons.Default.Close, contentDescription = "Ê∏ÖÈô§?ÔøΩÔøΩ?")
                             }
                         }
                     },
-                    placeholder = { Text("?úÂ?Ê®ôÈ??ÅÊ?‰ª∂Â?Á®±Ê?ËøëÊ??ãÂ??ÑÁ?Ë®?) },
+                    placeholder = { Text("?ÔøΩÔøΩ?Ê®ôÔøΩ??ÔøΩÔøΩ?‰ª∂ÔøΩ?Á®±ÔøΩ?ËøëÔøΩ??ÔøΩÔøΩ??ÔøΩÔøΩ?Ôø?) },
                     shape = RoundedCornerShape(24.dp),
                     modifier = Modifier.fillMaxWidth()
                 )
@@ -726,12 +727,12 @@ private fun LibraryHeroPanel(
                     verticalAlignment = Alignment.CenterVertically
                 ) {
                     LibraryStatPill(
-                        title = "?ÆÂ?È°ØÁ§∫",
-                        value = "$visibleDocuments ‰ª?
+                        title = "?ÔøΩÔøΩ?È°ØÁ§∫",
+                        value = "$visibleDocuments Ôø?
                     )
                     LibraryStatPill(
-                        title = if (searchQuery.isBlank()) "?Ä?? else "?úÂ?Ê®°Â?",
-                        value = if (searchQuery.isBlank()) "Â∑•‰??∞Â??? else "Â∑≤Â??®È??µÂ?"
+                        title = if (searchQuery.isBlank()) "?Ôø?? else "?ÔøΩÔøΩ?Ê®°ÔøΩ?",
+                        value = if (searchQuery.isBlank()) "Â∑•ÔøΩ??ÔøΩÔøΩ??? else "Â∑≤ÔøΩ??ÔøΩÔøΩ??ÔøΩÔøΩ?"
                     )
                 }
             }
@@ -796,8 +797,8 @@ private fun DocumentLibraryFab(
                     Icon(Icons.Default.Add, contentDescription = null, tint = Color.White)
                 }
                 Column {
-                    Text("?∞Â?Á≠ÜË?", color = Color.White, style = MaterialTheme.typography.labelLarge)
-                    Text("Á©∫ÁôΩ?ÅÊ??ØÂÖ• PDF", color = Color.White.copy(alpha = 0.82f), style = MaterialTheme.typography.labelSmall)
+                    Text("?ÔøΩÔøΩ?Á≠ÜÔøΩ?", color = Color.White, style = MaterialTheme.typography.labelLarge)
+                    Text("Á©∫ÁôΩ?ÔøΩÔøΩ??ÔøΩÂÖ• PDF", color = Color.White.copy(alpha = 0.82f), style = MaterialTheme.typography.labelSmall)
                 }
             }
         }
@@ -807,12 +808,12 @@ private fun DocumentLibraryFab(
             onDismissRequest = onDismissMenu
         ) {
             androidx.compose.material3.DropdownMenuItem(
-                text = { Text("?ãÂ? PDF") },
+                text = { Text("?ÔøΩÔøΩ? PDF") },
                 leadingIcon = { Icon(Icons.Default.Description, contentDescription = null) },
                 onClick = onOpenPdf
             )
             androidx.compose.material3.DropdownMenuItem(
-                text = { Text("Á©∫ÁôΩÁ≠ÜË?") },
+                text = { Text("Á©∫ÁôΩÁ≠ÜÔøΩ?") },
                 leadingIcon = { Icon(Icons.Filled.Add, contentDescription = null) },
                 onClick = onCreateBlank
             )
@@ -891,15 +892,15 @@ private fun LibraryEmptyState(
                     }
 
                     Text(
-                        text = if (isSearchActive) "Ê≤íÊ??æÂà∞Á¨¶Â? \"$searchQuery\" ?ÑÁ?Ë®? else "?àÊ?Â∑•‰??∞Â∑≤Á∂ìÂ∞±‰Ω?,
+                        text = if (isSearchActive) "Ê≤íÔøΩ??ÔøΩÂà∞Á¨¶ÔøΩ? \"$searchQuery\" ?ÔøΩÔøΩ?Ôø? else "?ÔøΩÔøΩ?Â∑•ÔøΩ??ÔøΩÂ∑≤Á∂ìÂ∞±Ôø?,
                         style = MaterialTheme.typography.headlineSmall,
                         color = MaterialTheme.colorScheme.onBackground
                     )
                     Text(
                         text = if (isSearchActive) {
-                            "?Ø‰ª•Ê∏ÖÈô§?úÂ?Ê¢ù‰ª∂ÔºåÊ??¥Êé•Âª∫Á??∞Á??âÁ®ø?ÅÔ??äÈÄôÁ??≥Ê??àÂ?‰∏ã‰???
+                            "?ÔøΩ‰ª•Ê∏ÖÈô§?ÔøΩÔøΩ?Ê¢ù‰ª∂ÔºåÔøΩ??ÔøΩÊé•Âª∫ÔøΩ??ÔøΩÔøΩ??ÔøΩÁ®ø?ÔøΩÔøΩ??ÔøΩÈÄôÔøΩ??ÔøΩÔøΩ??ÔøΩÔøΩ?‰∏ãÔøΩ???
                         } else {
-                            "ÂæûÈÄôË£°?ãÂ?Á¨¨‰?‰ª?PDFÔºåÊ??¥Êé•Âª∫Á?‰∏ÄÂºµÁ©∫?ΩÈ?ÔºåÈ?Âßã‰??ÑË®ªË®òÊ?Á®ã„Ä?
+                            "ÂæûÈÄôË£°?ÔøΩÔøΩ?Á¨¨ÔøΩ?Ôø?PDFÔºåÔøΩ??ÔøΩÊé•Âª∫ÔøΩ?‰∏ÄÂºµÁ©∫?ÔøΩÔøΩ?ÔºåÔøΩ?ÂßãÔøΩ??ÔøΩË®ªË®òÔøΩ?Á®ãÔøΩ?
                         },
                         style = MaterialTheme.typography.bodyMedium,
                         color = MaterialTheme.colorScheme.onSurfaceVariant
@@ -916,7 +917,7 @@ private fun LibraryEmptyState(
                                 modifier = Modifier.clickable(onClick = onClearSearch)
                             ) {
                                 Text(
-                                    text = "Ê∏ÖÈô§?úÂ?",
+                                    text = "Ê∏ÖÈô§?ÔøΩÔøΩ?",
                                     modifier = Modifier.padding(horizontal = 18.dp, vertical = 12.dp),
                                     style = MaterialTheme.typography.labelLarge,
                                     color = MaterialTheme.colorScheme.onPrimaryContainer
@@ -930,7 +931,7 @@ private fun LibraryEmptyState(
                                     .clickable(onClick = onOpenPdf)
                             ) {
                                 Text(
-                                    text = "?ãÂ? PDF",
+                                    text = "?ÔøΩÔøΩ? PDF",
                                     modifier = Modifier.padding(horizontal = 18.dp, vertical = 12.dp),
                                     style = MaterialTheme.typography.labelLarge,
                                     color = Color.White
@@ -944,7 +945,7 @@ private fun LibraryEmptyState(
                             modifier = Modifier.clickable(onClick = onCreateBlank)
                         ) {
                             Text(
-                                text = "Âª∫Á?Á©∫ÁôΩÁ≠ÜË?",
+                                text = "Âª∫ÔøΩ?Á©∫ÁôΩÁ≠ÜÔøΩ?",
                                 modifier = Modifier.padding(horizontal = 18.dp, vertical = 12.dp),
                                 style = MaterialTheme.typography.labelLarge,
                                 color = MaterialTheme.colorScheme.onSurface
@@ -979,13 +980,13 @@ private fun DocumentCard(
     if (showRenameDialog) {
         androidx.compose.material3.AlertDialog(
             onDismissRequest = { showRenameDialog = false },
-            title = { Text("?çÊñ∞?ΩÂ?") },
+            title = { Text("?ÔøΩÊñ∞?ÔøΩÔøΩ?") },
             text = {
                 androidx.compose.material3.OutlinedTextField(
                     value = renameInput,
                     onValueChange = { renameInput = it },
                     singleLine = true,
-                    label = { Text("Á≠ÜË??çÁ®±") },
+                    label = { Text("Á≠ÜÔøΩ??ÔøΩÁ®±") },
                     modifier = Modifier.fillMaxWidth()
                 )
             },
@@ -996,11 +997,11 @@ private fun DocumentCard(
                         showRenameDialog = false
                     },
                     enabled = renameInput.isNotBlank()
-                ) { Text("Á¢∫Ë?") }
+                ) { Text("Á¢∫ÔøΩ?") }
             },
             dismissButton = {
                 androidx.compose.material3.TextButton(onClick = { showRenameDialog = false }) {
-                    Text("?ñÊ?")
+                    Text("?ÔøΩÔøΩ?")
                 }
             }
         )
@@ -1009,9 +1010,9 @@ private fun DocumentCard(
     if (showDeleteDialog) {
         androidx.compose.material3.AlertDialog(
             onDismissRequest = { showDeleteDialog = false },
-            title = { Text("?™Èô§Á≠ÜË???) },
+            title = { Text("?ÔøΩÈô§Á≠ÜÔøΩ???) },
             text = {
-                Text("Á¢∫Â?Ë¶ÅÂà™?§„Ä?{document.displayName}?çÂ?ÔºüÊ≠§?ç‰??É‰?‰ΩµÁßª?§Á?Ë∑°Ë?Ê®ôË®ªÔºå‰??°Ê?Âæ©Â???)
+                Text("Á¢∫ÔøΩ?Ë¶ÅÂà™?ÔøΩÔøΩ?{document.displayName}?ÔøΩÔøΩ?ÔºüÊ≠§?ÔøΩÔøΩ??ÔøΩÔøΩ?‰ΩµÁßª?ÔøΩÔøΩ?Ë∑°ÔøΩ?Ê®ôË®ªÔºåÔøΩ??ÔøΩÔøΩ?Âæ©ÔøΩ???)
             },
             confirmButton = {
                 androidx.compose.material3.TextButton(
@@ -1020,12 +1021,12 @@ private fun DocumentCard(
                         onDelete()
                     }
                 ) {
-                    Text("?™Èô§", color = MaterialTheme.colorScheme.error)
+                    Text("?ÔøΩÈô§", color = MaterialTheme.colorScheme.error)
                 }
             },
             dismissButton = {
                 androidx.compose.material3.TextButton(onClick = { showDeleteDialog = false }) {
-                    Text("?ñÊ?")
+                    Text("?ÔøΩÔøΩ?")
                 }
             }
         )
@@ -1137,7 +1138,7 @@ private fun DocumentCard(
                             onDismissRequest = { showMenu = false }
                         ) {
                             androidx.compose.material3.DropdownMenuItem(
-                                text = { Text("?çÊñ∞?ΩÂ?") },
+                                text = { Text("?ÔøΩÊñ∞?ÔøΩÔøΩ?") },
                                 leadingIcon = { Icon(Icons.Default.Create, contentDescription = null) },
                                 onClick = {
                                     showMenu = false
@@ -1146,7 +1147,7 @@ private fun DocumentCard(
                                 }
                             )
                             androidx.compose.material3.DropdownMenuItem(
-                                text = { Text("?™Èô§") },
+                                text = { Text("?ÔøΩÈô§") },
                                 leadingIcon = { Icon(Icons.Default.Delete, contentDescription = null) },
                                 onClick = {
                                     showMenu = false
@@ -1169,6 +1170,7 @@ private fun DocumentCard(
 
 // --- 3. Editor Screen ---
 enum class SidebarMode { COLLAPSED, NORMAL, FULLSCREEN }
+enum class PageViewMode { SINGLE_PAGE, CONTINUOUS_SCROLL }
 
 @Composable
 fun TabletEditorScreen(navController: NavController, uri: Uri, db: AppDatabase) {
@@ -1190,11 +1192,16 @@ fun TabletEditorScreen(navController: NavController, uri: Uri, db: AppDatabase)
         factory = DocumentViewModelFactory(db.documentDao(), db.strokeDao(), db)
     )
     var sidebarMode by rememberSaveable { mutableStateOf(SidebarMode.NORMAL) }
+    var pageViewMode by rememberSaveable { mutableStateOf(PageViewMode.SINGLE_PAGE) }
     val activeTool by viewModel.selectedTool.collectAsState()
     var currentPageIndex by rememberSaveable { mutableIntStateOf(0) }
     var showStrokeWidthSlider by rememberSaveable { mutableStateOf(false) }
     // Guards against overwriting the DB value before we've read it on first open
     var initialPageRestored by rememberSaveable { mutableStateOf(false) }
+    
+    var showAiPanel by rememberSaveable { mutableStateOf(false) }
+    var aiFileUri by rememberSaveable { mutableStateOf<Uri?>(null) }
+    
     val sidebarListState = rememberLazyListState()
 
     // Restore the last-viewed page from DB on first open; rememberSaveable keeps it
@@ -1267,12 +1274,16 @@ fun TabletEditorScreen(navController: NavController, uri: Uri, db: AppDatabase)
             documentTitle = documentTitle,
             pageCount = pageCount,
             currentPageIndex = currentPageIndex,
+            currentViewMode = pageViewMode,
             isPageOperationInProgress = isPageOperationInProgress,
             currentStyle = paperStyle,
             onDismiss = { showDocumentSettingsDialog = false },
             onConfirmStyle = { newStyle ->
                 viewModel.setPaperStyle(newStyle)
             },
+            onConfirmViewMode = { mode ->
+                pageViewMode = mode
+            },
             onInsertPdf = {
                 showDocumentSettingsDialog = false
                 insertPdfLauncher.launch(arrayOf("application/pdf"))
@@ -1357,133 +1368,287 @@ fun TabletEditorScreen(navController: NavController, uri: Uri, db: AppDatabase)
             pdfViewModel.consumeDeletedPageEvent()
         }
 
-        Row(Modifier.fillMaxSize()) {
-            val isFullscreen = sidebarMode == SidebarMode.FULLSCREEN
-            Sidebar(
-                sidebarMode = sidebarMode,
-                onModeChange = { sidebarMode = it },
-                pdfViewModel = pdfViewModel,
+        Row(modifier = Modifier.fillMaxSize()) {
+            // Left Side AI Web Panel
+            androidx.compose.animation.AnimatedVisibility(
+                visible = showAiPanel,
+                modifier = if (showAiPanel) Modifier.weight(0.4f).fillMaxHeight() else Modifier
+            ) {
+                AiWebPanel(
+                    fileUri = aiFileUri,
+                    onClose = { showAiPanel = false }
+                )
+            }
+            
+            // Right Side Editor Workspace
+            Box(modifier = if (showAiPanel) Modifier.weight(0.6f).fillMaxHeight() else Modifier.weight(1f).fillMaxHeight()) {
+                if (pageViewMode == PageViewMode.SINGLE_PAGE) {
+            Row(Modifier.fillMaxSize()) {
+                val isFullscreen = sidebarMode == SidebarMode.FULLSCREEN
+                Sidebar(
+                    sidebarMode = sidebarMode,
+                    onModeChange = { sidebarMode = it },
+                    pdfViewModel = pdfViewModel,
+                    modelWidthPt = paperStyle.widthPt,
+                    modelHeightPt = paperStyle.heightPt,
+                    pageCount = pageCount,
+                    currentPageIndex = currentPageIndex,
+                    db = db,
+                    documentUri = uri.toString(),
+                    onPageSelected = { index ->
+                        currentPageIndex = index
+                        viewModel.setActivePage(index)
+                    },
+                    onAddPage = { afterIndex ->
+                        scope.launch {
+                            pdfViewModel.insertBlankPage(
+                                context,
+                                uri.toString(),
+                                afterIndex,
+                                pageWidthPt = paperStyle.widthPt,
+                                pageHeightPt = paperStyle.heightPt
+                            )
+                        }
+                    },
+                    onDeletePages = { indices ->
+                        pdfViewModel.deletePages(uri.toString(), indices)
+                    },
+                    listState = sidebarListState,
+                    modifier = if (isFullscreen) Modifier.fillMaxHeight().weight(1f) else Modifier.fillMaxHeight()
+                )
+
+                if (!isFullscreen) {
+                    Box(Modifier.weight(1f).fillMaxHeight()) {
+                        Workspace(
+                            pageIndex = currentPageIndex,
+                            pdfViewModel = pdfViewModel,
+                            viewModel = viewModel,
+                            modifier = Modifier.fillMaxSize(),
+                            pageAspectRatio = pageAspectRatio
+                        )
+                        SidebarFloatingControls(
+                            sidebarMode = sidebarMode,
+                            onModeChange = { sidebarMode = it },
+                            modifier = Modifier.align(Alignment.CenterStart).padding(start = 4.dp)
+                        )
+                        
+                        // Selected region bubble (Extract button) over Workspace
+                        val lassoPolygon by viewModel.lassoPolygon.collectAsState()
+                        var isExtracting by remember { mutableStateOf(false) }
+
+                        androidx.compose.animation.AnimatedVisibility(
+                            visible = lassoPolygon.isNotEmpty() && !isExtracting,
+                            modifier = Modifier.align(Alignment.TopCenter).padding(top = 16.dp),
+                            enter = fadeIn() + slideInVertically(initialOffsetY = { -it }),
+                            exit = fadeOut() + slideOutVertically(targetOffsetY = { -it })
+                        ) {
+                            Surface(
+                                shape = CircleShape,
+                                color = MaterialTheme.colorScheme.primaryContainer,
+                                contentColor = MaterialTheme.colorScheme.onPrimaryContainer,
+                                shadowElevation = 8.dp
+                            ) {
+                                Row(
+                                    modifier = Modifier.padding(horizontal = 16.dp, vertical = 8.dp),
+                                    horizontalArrangement = Arrangement.spacedBy(8.dp),
+                                    verticalAlignment = Alignment.CenterVertically
+                                ) {
+                                    Text(
+                                        text = "Â∑≤ÈÅ∏?ÔøΩÔøΩ???,
+                                        style = MaterialTheme.typography.labelLarge
+                                    )
+                                    androidx.compose.material3.TextButton(
+                                        enabled = !isExtracting,
+                                        onClick = {
+                                            if (isExtracting) return@TextButton
+                                            isExtracting = true
+                                            scope.launch {
+                                                try {
+                                                    // Snapshot source page first; page index may auto-switch after insertion.
+                                                    val sourcePageIndex = currentPageIndex
+                                                    val sourceBitmap = kotlinx.coroutines.withTimeoutOrNull(1200) {
+                                                        pdfViewModel.getPageBitmap(sourcePageIndex)
+                                                            .filterNotNull()
+                                                            .first()
+                                                    } ?: pdfViewModel.getPageBitmap(sourcePageIndex).value
+
+                                                    // Insert blank page right after source page.
+                                                    val newPageIndex = sourcePageIndex + 1
+                                                    pdfViewModel.insertBlankPage(context, uri.toString(), sourcePageIndex)
+
+                                                    // Extract source region and place it onto the new page.
+                                                    viewModel.extractRegionToNewPage(
+                                                        context = context,
+                                                        sourcePageIndex = sourcePageIndex,
+                                                        targetPageIndex = newPageIndex,
+                                                        pdfPageBitmap = sourceBitmap
+                                                    )
+                                                } finally {
+                                                    isExtracting = false
+                                                }
+                                            }
+                                        }
+                                    ) {
+                                        Text("?ÔøΩÔøΩ??ÔøΩÊñ∞?ÔøΩÈù¢")
+                                    }
+                                    androidx.compose.material3.TextButton(
+                                        enabled = !isExtracting,
+                                        onClick = {
+                                            if (isExtracting) return@TextButton
+                                            isExtracting = true
+                                            scope.launch {
+                                                try {
+                                                    val sourcePageIndex = currentPageIndex
+                                                    val sourceBitmap = kotlinx.coroutines.withTimeoutOrNull(1200) {
+                                                        pdfViewModel.getPageBitmap(sourcePageIndex).filterNotNull().first()
+                                                    } ?: pdfViewModel.getPageBitmap(sourcePageIndex).value
+
+                                                    val sharedFile = viewModel.extractRegionToShareFile(
+                                                        context = context,
+                                                        sourcePageIndex = sourcePageIndex,
+                                                        pdfPageBitmap = sourceBitmap
+                                                    )
+                                                    
+                                                    if (sharedFile != null) {
+                                                        val uriForFile = androidx.core.content.FileProvider.getUriForFile(
+                                                            context,
+                                                            "${context.packageName}.fileprovider",
+                                                            sharedFile
+                                                        )
+                                                        val shareIntent = android.content.Intent(android.content.Intent.ACTION_SEND).apply {
+                                                            type = "image/png"
+                                                            putExtra(android.content.Intent.EXTRA_STREAM, uriForFile)
+                                                            putExtra(android.content.Intent.EXTRA_TEXT, "?ÔøΩÂºµ?ÔøΩÔøΩ?Ë£°ÔøΩ?‰ªÄÈ∫ºÔøΩ?Ë´ãÂπ´?ÔøΩÔøΩ??ÔøΩÔøΩ?ÈªûÔøΩ?)
+                                                            addFlags(android.content.Intent.FLAG_GRANT_READ_URI_PERMISSION)
+                                                        }
+                                                        context.startActivity(android.content.Intent.createChooser(shareIntent, "‰ΩøÁî® AI Ôø???Ôø??.."))
+                                                    }
+                                                } finally {
+                                                    isExtracting = false
+                                                }
+                                            }
+                                        }
+                                    ) {
+                                        Text("AI Ôø??")
+                                    }
+                                    androidx.compose.material3.IconButton(
+                                        onClick = { viewModel.clearSelection() },
+                                        modifier = Modifier.size(24.dp)
+                                    ) {
+                                        Icon(
+                                            Icons.Default.Close,
+                                            contentDescription = "?ÔøΩÔøΩ??ÔøΩÔøΩ?",
+                                            modifier = Modifier.size(16.dp)
+                                        )
+                                    }
+                                }
+                            }
+                        }
+                    }
+                }
+            }
+        } else {
+            ContinuousPdfWorkspace(
                 pageCount = pageCount,
                 currentPageIndex = currentPageIndex,
-                db = db,
-                documentUri = uri.toString(),
+                pdfViewModel = pdfViewModel,
                 onPageSelected = { index ->
                     currentPageIndex = index
                     viewModel.setActivePage(index)
                 },
-                onAddPage = { afterIndex ->
-                    scope.launch {
-                        pdfViewModel.insertBlankPage(
-                            context,
-                            uri.toString(),
-                            afterIndex,
-                            pageWidthPt = paperStyle.widthPt,
-                            pageHeightPt = paperStyle.heightPt
-                        )
-                    }
-                },
-                onDeletePage = { index ->
-                    // DB Ê∏ÖÁ???PDF ?™Èô§Âπ≥Ë??∑Ë?ÔºåÁ∏Æ?≠Á∏ΩÁ≠âÂ??ÇÈ?
-                    scope.launch(kotlinx.coroutines.Dispatchers.IO) {
-                        db.strokeDao().clearPage(uri.toString(), index)
-                        db.strokeDao().shiftPageIndicesDown(uri.toString(), index)
-                        db.imageAnnotationDao().deleteForPage(uri.toString(), index)
-                        db.imageAnnotationDao().shiftPageIndicesDown(uri.toString(), index)
-                        db.textAnnotationDao().deleteForPage(uri.toString(), index)
-                        db.textAnnotationDao().shiftPageIndicesDown(uri.toString(), index)
-                    }
-                    pdfViewModel.deletePage(index)
-                },
-                listState = sidebarListState,
-                modifier = if (isFullscreen) Modifier.fillMaxHeight().weight(1f) else Modifier.fillMaxHeight()
+                modifier = Modifier.fillMaxSize()
             )
+        }
+            } // closes wrapper Box
+        } // closes wrapper Row
+    }
+}
 
-            if (!isFullscreen) {
-                Box(Modifier.weight(1f).fillMaxHeight()) {
-                    Workspace(
-                        pageIndex = currentPageIndex,
-                        pdfViewModel = pdfViewModel,
-                        viewModel = viewModel,
-                        modifier = Modifier.fillMaxSize(),
-                        pageAspectRatio = pageAspectRatio
-                    )
-                    SidebarFloatingControls(
-                        sidebarMode = sidebarMode,
-                        onModeChange = { sidebarMode = it },
-                        modifier = Modifier.align(Alignment.CenterStart).padding(start = 4.dp)
-                    )
-                    
-                    // Selected region bubble (Extract button) over Workspace
-                    val lassoPolygon by viewModel.lassoPolygon.collectAsState()
-                    var isExtracting by remember { mutableStateOf(false) }
-
-                    androidx.compose.animation.AnimatedVisibility(
-                        visible = lassoPolygon.isNotEmpty() && !isExtracting,
-                        modifier = Modifier.align(Alignment.TopCenter).padding(top = 16.dp),
-                        enter = fadeIn() + slideInVertically(initialOffsetY = { -it }),
-                        exit = fadeOut() + slideOutVertically(targetOffsetY = { -it })
+@Composable
+private fun ContinuousPdfWorkspace(
+    pageCount: Int,
+    currentPageIndex: Int,
+    pdfViewModel: PdfViewModel,
+    onPageSelected: (Int) -> Unit,
+    modifier: Modifier = Modifier
+) {
+    val isDarkSurface = MaterialTheme.colorScheme.background.luminance() < 0.5f
+    val deskColor = if (isDarkSurface) WorkspaceDeskDark else WorkspaceDeskLight
+    val paperColor = if (isDarkSurface) PaperDark else PaperLight
+    val horizontalScrollState = rememberScrollState()
+
+    BoxWithConstraints(
+        modifier = modifier
+            .background(deskColor)
+            .horizontalScroll(horizontalScrollState)
+    ) {
+        val contentWidth = maxWidth + 320.dp
+        val pageWidth = (maxWidth * 0.82f).coerceAtMost(920.dp)
+
+        Box(
+            modifier = Modifier
+                .width(contentWidth)
+                .fillMaxHeight()
+                .padding(horizontal = 28.dp, vertical = 24.dp),
+            contentAlignment = Alignment.TopCenter
+        ) {
+            LazyColumn(
+                modifier = Modifier.fillMaxHeight(),
+                contentPadding = PaddingValues(bottom = 48.dp),
+                verticalArrangement = Arrangement.spacedBy(20.dp),
+                horizontalAlignment = Alignment.CenterHorizontally
+            ) {
+                items(pageCount, key = { it }) { index ->
+                    val pageBitmapFlow = remember(index) { pdfViewModel.getPageBitmap(index) }
+                    val pageBitmap by pageBitmapFlow.collectAsState()
+                    val pageBorderColor = if (index == currentPageIndex) {
+                        MaterialTheme.colorScheme.primary
+                    } else {
+                        MaterialTheme.colorScheme.outlineVariant
+                    }
+                    val pageBorderWidth = if (index == currentPageIndex) 2.dp else 1.dp
+                    val pageAspectRatio = pdfViewModel.getPageAspectRatio(index)
+
+                    Column(
+                        horizontalAlignment = Alignment.CenterHorizontally,
+                        verticalArrangement = Arrangement.spacedBy(8.dp)
                     ) {
                         Surface(
-                            shape = CircleShape,
-                            color = MaterialTheme.colorScheme.primaryContainer,
-                            contentColor = MaterialTheme.colorScheme.onPrimaryContainer,
-                            shadowElevation = 8.dp
+                            modifier = Modifier
+                                .width(pageWidth)
+                                .aspectRatio(pageAspectRatio)
+                                .clickable { onPageSelected(index) },
+                            shape = RoundedCornerShape(10.dp),
+                            shadowElevation = 18.dp,
+                            color = paperColor,
+                            border = BorderStroke(pageBorderWidth, pageBorderColor)
                         ) {
-                            Row(
-                                modifier = Modifier.padding(horizontal = 16.dp, vertical = 8.dp),
-                                horizontalArrangement = Arrangement.spacedBy(8.dp),
-                                verticalAlignment = Alignment.CenterVertically
-                            ) {
-                                Text(
-                                    text = "Â∑≤ÈÅ∏?ñÂ???,
-                                    style = MaterialTheme.typography.labelLarge
-                                )
-                                androidx.compose.material3.TextButton(
-                                    enabled = !isExtracting,
-                                    onClick = {
-                                        if (isExtracting) return@TextButton
-                                        isExtracting = true
-                                        scope.launch {
-                                            try {
-                                                // Snapshot source page first; page index may auto-switch after insertion.
-                                                val sourcePageIndex = currentPageIndex
-                                                val sourceBitmap = kotlinx.coroutines.withTimeoutOrNull(1200) {
-                                                    pdfViewModel.getPageBitmap(sourcePageIndex)
-                                                        .filterNotNull()
-                                                        .first()
-                                                } ?: pdfViewModel.getPageBitmap(sourcePageIndex).value
-
-                                                // Insert blank page right after source page.
-                                                val newPageIndex = sourcePageIndex + 1
-                                                pdfViewModel.insertBlankPage(context, uri.toString(), sourcePageIndex)
-
-                                                // Extract source region and place it onto the new page.
-                                                viewModel.extractRegionToNewPage(
-                                                    context = context,
-                                                    sourcePageIndex = sourcePageIndex,
-                                                    targetPageIndex = newPageIndex,
-                                                    pdfPageBitmap = sourceBitmap
-                                                )
-                                            } finally {
-                                                isExtracting = false
-                                            }
-                                        }
-                                    }
-                                ) {
-                                    Text("?êÂ??∞Êñ∞?ÅÈù¢")
-                                }
-                                androidx.compose.material3.IconButton(
-                                    onClick = { viewModel.clearSelection() },
-                                    modifier = Modifier.size(24.dp)
-                                ) {
-                                    Icon(
-                                        Icons.Default.Close,
-                                        contentDescription = "?ñÊ??∏Â?",
-                                        modifier = Modifier.size(16.dp)
+                            Box(modifier = Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
+                                if (pageBitmap != null) {
+                                    Image(
+                                        bitmap = pageBitmap!!.asImageBitmap(),
+                                        contentDescription = "PDF Page ${index + 1}",
+                                        modifier = Modifier.fillMaxSize(),
+                                        contentScale = androidx.compose.ui.layout.ContentScale.Fit
+                                    )
+                                } else {
+                                    Text(
+                                        text = "ËºâÂÖ•‰∏≠ÔøΩ?,
+                                        style = MaterialTheme.typography.bodyMedium,
+                                        color = MaterialTheme.colorScheme.onSurfaceVariant
                                     )
                                 }
                             }
                         }
+                        Text(
+                            text = "Ôø?${index + 1} ??,
+                            style = MaterialTheme.typography.labelMedium,
+                            color = if (index == currentPageIndex) {
+                                MaterialTheme.colorScheme.primary
+                            } else {
+                                MaterialTheme.colorScheme.onSurfaceVariant
+                            }
+                        )
                     }
                 }
             }
@@ -1497,38 +1662,41 @@ private fun Sidebar(
     sidebarMode: SidebarMode,
     onModeChange: (SidebarMode) -> Unit,
     pdfViewModel: PdfViewModel,
+    modelWidthPt: Float,
+    modelHeightPt: Float,
     pageCount: Int,
     currentPageIndex: Int,
     db: AppDatabase,
     documentUri: String,
     onPageSelected: (Int) -> Unit,
     onAddPage: (afterIndex: Int) -> Unit,
-    onDeletePage: (Int) -> Unit,
+    onDeletePages: (List<Int>) -> Unit,
     listState: LazyListState = rememberLazyListState(),
     modifier: Modifier = Modifier
 ) {
-    // Index of the page pending delete confirmation; null = no dialog shown
-    var deleteConfirmIndex by remember { mutableStateOf<Int?>(null) }
+    // Indices of the pages pending delete confirmation; empty = no dialog shown
+    var deleteConfirmIndices by remember { mutableStateOf<List<Int>>(emptyList()) }
     val thumbnailVersion by pdfViewModel.thumbnailVersion.collectAsState()
     val isPageOperationInProgress by pdfViewModel.isPageOperationInProgress.collectAsState()
-    if (deleteConfirmIndex != null) {
-        val idx = deleteConfirmIndex!!
+    if (deleteConfirmIndices.isNotEmpty()) {
+        val count = deleteConfirmIndices.size
         androidx.compose.material3.AlertDialog(
-            onDismissRequest = { deleteConfirmIndex = null },
-            title = { Text("?™Èô§?ÅÈù¢") },
-            text = { Text("Á¢∫Â?Ë¶ÅÂà™?§Á¨¨ ${idx + 1} ?ÅÔ?Ê≠§Ê?‰ΩúÁÑ°Ê≥ïÂæ©?ü„Ä?) },
+            onDismissRequest = { deleteConfirmIndices = emptyList() },
+            title = { Text("?ÔøΩÈô§?ÔøΩÈù¢") },
+            text = { Text("Á¢∫ÔøΩ?Ë¶ÅÂà™?ÔøΩÈÅ∏?ÔøΩÔøΩ? $count ?ÔøΩÔøΩ?Ê≠§ÔøΩ?‰ΩúÁÑ°Ê≥ïÂæ©?ÔøΩÔøΩ?) },
             confirmButton = {
                 androidx.compose.material3.TextButton(
                     onClick = {
-                        deleteConfirmIndex = null
-                        onDeletePage(idx)
+                        val toDelete = deleteConfirmIndices
+                        deleteConfirmIndices = emptyList()
+                        onDeletePages(toDelete)
                     },
                     enabled = !isPageOperationInProgress
-                ) { Text("?™Èô§", color = MaterialTheme.colorScheme.error) }
+                ) { Text("?ÔøΩÈô§", color = MaterialTheme.colorScheme.error) }
             },
             dismissButton = {
-                androidx.compose.material3.TextButton(onClick = { deleteConfirmIndex = null }) {
-                    Text("?ñÊ?")
+                androidx.compose.material3.TextButton(onClick = { deleteConfirmIndices = emptyList() }) {
+                    Text("?ÔøΩÔøΩ?")
                 }
             }
         )
@@ -1554,7 +1722,10 @@ private fun Sidebar(
         border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant.copy(alpha = 0.55f))
     ) {
         if (sidebarMode == SidebarMode.FULLSCREEN) {
-            // Fullscreen: 4-column page grid with back button
+            // Fullscreen: Multi-select & Responsive Grid
+            var selectionMode by remember { mutableStateOf(false) }
+            val selectedPages = remember { mutableStateListOf<Int>() }
+            
             val gridState = androidx.compose.foundation.lazy.grid.rememberLazyGridState()
             LaunchedEffect(Unit) {
                 // Scroll so the current page is visible when the grid opens
@@ -1562,28 +1733,88 @@ private fun Sidebar(
                 gridState.scrollToItem(index = (row * 4).coerceAtLeast(0))
             }
             Column(Modifier.fillMaxSize()) {
+                // Header Bar for Fullscreen Mode
                 Row(
-                    modifier = Modifier.fillMaxWidth().height(48.dp).padding(horizontal = 8.dp),
+                    modifier = Modifier.fillMaxWidth().height(56.dp).padding(horizontal = 8.dp),
                     verticalAlignment = Alignment.CenterVertically
                 ) {
-                    IconButton(onClick = { onModeChange(SidebarMode.NORMAL) }) {
-                        Icon(Icons.AutoMirrored.Filled.ArrowBack, contentDescription = "Exit grid view")
+                    IconButton(onClick = {
+                        if (selectionMode) {
+                            selectionMode = false
+                            selectedPages.clear()
+                        } else {
+                            onModeChange(SidebarMode.NORMAL)
+                        }
+                    }) {
+                        Icon(
+                            if (selectionMode) Icons.Default.Close else Icons.AutoMirrored.Filled.ArrowBack, 
+                            contentDescription = if (selectionMode) "?ÔøΩÔøΩ??ÔøΩÔøΩ?" else "?ÔøΩÔøΩ?Á∂≤Ê†ºÊ™¢ÔøΩ?"
+                        )
+                    }
+                    
+                    if (selectionMode) {
+                        Text(
+                            text = "Â∑≤ÈÅ∏??${selectedPages.size} ??,
+                            style = MaterialTheme.typography.titleMedium,
+                            modifier = Modifier.weight(1f)
+                        )
+                        // ?ÔøΩÔøΩ?ÁßªÔøΩ??ÔøΩÔøΩ?
+                        if (selectedPages.size == 1) {
+                            val selectedIdx = selectedPages.first()
+                            IconButton(
+                                onClick = { 
+                                    pdfViewModel.movePage(documentUri, selectedIdx, selectedIdx - 1)
+                                    selectedPages[0] = selectedIdx - 1
+                                },
+                                enabled = selectedIdx > 0 && !isPageOperationInProgress
+                            ) {
+                                Icon(Icons.AutoMirrored.Filled.ArrowBack, "ÂæÄ?ÔøΩÁßª")
+                            }
+                            IconButton(
+                                onClick = { 
+                                    pdfViewModel.movePage(documentUri, selectedIdx, selectedIdx + 1)
+                                    selectedPages[0] = selectedIdx + 1
+                                },
+                                enabled = selectedIdx < pageCount - 1 && !isPageOperationInProgress
+                            ) {
+                                Icon(Icons.AutoMirrored.Filled.ArrowForward, "ÂæÄÂæåÁßª")
+                            }
+                        }
+                        // ?ÔøΩÈô§?ÔøΩÔøΩ?
+                        if (selectedPages.isNotEmpty()) {
+                            IconButton(
+                                onClick = {
+                                    deleteConfirmIndices = selectedPages.toList()
+                                    selectionMode = false
+                                    selectedPages.clear()
+                                },
+                                enabled = selectedPages.isNotEmpty() && pageCount > selectedPages.size
+                            ) {
+                                Icon(Icons.Default.Delete, "?ÔøΩÈô§")
+                            }
+                        }
+                    } else {
+                        Text(
+                            text = "?Ôø?ÔøΩÔøΩ???,
+                            style = MaterialTheme.typography.titleMedium,
+                            modifier = Modifier.weight(1f)
+                        )
+                        // ?ÔøΩÂÖ•?ÔøΩÔøΩ?Ê®°ÔøΩ??ÔøΩÔøΩ???
+                        IconButton(onClick = { selectionMode = true }) {
+                            Icon(Icons.Default.CheckCircle, "?ÔøΩÂÖ•?ÔøΩÔøΩ?Ê®°ÔøΩ?")
+                        }
                     }
-                    Text(
-                        text = "?Ä?âÈ???,
-                        style = MaterialTheme.typography.titleSmall,
-                        modifier = Modifier.weight(1f)
-                    )
                 }
+                
                 LazyVerticalGrid(
-                    columns = GridCells.Fixed(4),
+                    columns = GridCells.Adaptive(minSize = 120.dp),
                     state = gridState,
                     contentPadding = PaddingValues(8.dp),
                     horizontalArrangement = Arrangement.spacedBy(8.dp),
                     verticalArrangement = Arrangement.spacedBy(8.dp),
                     modifier = Modifier.fillMaxSize()
                 ) {
-                    items(pageCount) { index ->
+                    items(pageCount, key = { it }) { index ->
                         val thumbFlow = androidx.compose.runtime.remember(index, thumbnailVersion) {
                             pdfViewModel.getPageThumbnail(index)
                         }
@@ -1600,28 +1831,58 @@ private fun Sidebar(
                             db.textAnnotationDao().getForPage(documentUri, index)
                         }
                         val texts by textsFlow.collectAsState(initial = emptyList())
-                        Box(modifier = Modifier.combinedClickable(
-                            onClick = {
-                                onPageSelected(index)
-                                onModeChange(SidebarMode.NORMAL)
-                            },
-                            onLongClick = { if (pageCount > 1 && !isPageOperationInProgress) deleteConfirmIndex = index }
-                        )) {
+                        
+                        val isItemSelected = selectedPages.contains(index)
+                        
+                        Box(modifier = Modifier
+                            .combinedClickable(
+                                onClick = {
+                                    if (selectionMode) {
+                                        if (isItemSelected) selectedPages.remove(index) else selectedPages.add(index)
+                                    } else {
+                                        onPageSelected(index)
+                                        onModeChange(SidebarMode.NORMAL)
+                                    }
+                                },
+                                onLongClick = { 
+                                    if (!selectionMode) {
+                                        selectionMode = true
+                                        selectedPages.add(index)
+                                    }
+                                }
+                            )
+                        ) {
                             PageThumbnail(
                                 pageIndex = index,
                                 bitmap = thumb,
                                 strokes = strokes,
                                 imageAnnotations = images,
                                 textAnnotations = texts,
-                                isSelected = index == currentPageIndex,
-                                boxModifier = Modifier.fillMaxWidth().aspectRatio(pdfViewModel.getPageAspectRatio(index))
+                                modelWidthPt = modelWidthPt,
+                                modelHeightPt = modelHeightPt,
+                                isSelected = if (selectionMode) isItemSelected else index == currentPageIndex,
+                                boxModifier = Modifier
+                                    .fillMaxWidth()
+                                    .aspectRatio(pdfViewModel.getPageAspectRatio(index))
+                                    .then(if (selectionMode && isItemSelected) Modifier.padding(4.dp).border(2.dp, MaterialTheme.colorScheme.primary, androidx.compose.foundation.shape.RoundedCornerShape(8.dp)) else Modifier)
                             )
+                            
+                            // ?ÔøΩÔøΩ?Ê®°ÔøΩ?‰∏ãÔøΩ??ÔøΩÔøΩ?ËßíÂãæ?ÔøΩÔøΩ?Ôø?
+                            if (selectionMode) {
+                                androidx.compose.material3.Checkbox(
+                                    checked = isItemSelected,
+                                    onCheckedChange = { checked ->
+                                        if (checked) selectedPages.add(index) else selectedPages.remove(index)
+                                    },
+                                    modifier = Modifier.align(Alignment.TopEnd).padding(4.dp)
+                                )
+                            }
                         }
                     }
                 }
             }
         } else {
-            // Normal / Collapsed ??thumbnail list + pinned add-page footer
+            // Normal / Collapsed ??thumbnail list + pinned add-page footer
             Column(Modifier.fillMaxSize()) {
                 LazyColumn(
                     state = listState,
@@ -1651,7 +1912,7 @@ private fun Sidebar(
                                 .padding(vertical = 8.dp)
                                 .combinedClickable(
                                     onClick = { onPageSelected(index) },
-                                    onLongClick = { if (pageCount > 1 && !isPageOperationInProgress) deleteConfirmIndex = index }
+                                    onLongClick = { if (pageCount > 1 && !isPageOperationInProgress) deleteConfirmIndices = listOf(index) }
                                 ),
                             contentAlignment = Alignment.Center
                         ) {
@@ -1662,6 +1923,8 @@ private fun Sidebar(
                                     strokes = strokes,
                                     imageAnnotations = images,
                                     textAnnotations = texts,
+                                    modelWidthPt = modelWidthPt,
+                                    modelHeightPt = modelHeightPt,
                                     isSelected = index == currentPageIndex,
                                     boxModifier = Modifier.width(88.dp).aspectRatio(pdfViewModel.getPageAspectRatio(index))
                                 )
@@ -1674,29 +1937,59 @@ private fun Sidebar(
                         }
                     }
                 }
-                // ?∫Â??®Â??®Á??∞Â??ÅÈù¢?âÈ?
+                // ?ÔøΩÔøΩ??ÔøΩÔøΩ??ÔøΩÔøΩ??ÔøΩÔøΩ??ÔøΩÈù¢?ÔøΩÔøΩ?
                 if (sidebarMode == SidebarMode.NORMAL) {
+                    var showTemplateMenu by remember { mutableStateOf(false) }
                     androidx.compose.material3.HorizontalDivider()
-                    // ?ÅÈù¢?ç‰??≤Ë?‰∏≠Ê?È°ØÁ§∫Á¥∞Èï∑?≤Â∫¶Ê¢ùÔ?Áµ¶‰?‰ΩøÁî®?ÖË?Ë¶∫Â?È•?+                    // ?ÔøΩÈù¢?ÔøΩÔøΩ??ÔøΩÔøΩ?‰∏≠ÔøΩ?È°ØÁ§∫Á¥∞Èï∑?ÔøΩÂ∫¶Ê¢ùÔøΩ?Áµ¶ÔøΩ?‰ΩøÁî®?ÔøΩÔøΩ?Ë¶∫ÔøΩ?Ôø?
                     if (isPageOperationInProgress) {
                         androidx.compose.material3.LinearProgressIndicator(
                             modifier = Modifier.fillMaxWidth().height(2.dp)
                         )
                     }
-                    androidx.compose.material3.TextButton(
-                        onClick = { onAddPage(currentPageIndex) },
-                        enabled = !isPageOperationInProgress,
-                        modifier = Modifier
-                            .fillMaxWidth()
-                            .padding(horizontal = 6.dp, vertical = 5.dp)
-                    ) {
-                        Icon(
-                            Icons.Default.Add,
-                            contentDescription = null,
-                            modifier = Modifier.size(16.dp)
-                        )
-                        Spacer(Modifier.width(4.dp))
-                        Text("?∞Â??ÅÈù¢", style = MaterialTheme.typography.labelMedium)
+                    Box(modifier = Modifier.fillMaxWidth()) {
+                        androidx.compose.material3.TextButton(
+                            onClick = { showTemplateMenu = true },
+                            enabled = !isPageOperationInProgress,
+                            modifier = Modifier
+                                .fillMaxWidth()
+                                .padding(horizontal = 6.dp, vertical = 5.dp)
+                        ) {
+                            Icon(
+                                Icons.Default.Add,
+                                contentDescription = null,
+                                modifier = Modifier.size(16.dp)
+                            )
+                            Spacer(Modifier.width(4.dp))
+                            Text("?ÔøΩÔøΩ??ÔøΩÈù¢", style = MaterialTheme.typography.labelMedium)
+                        }
+                        
+                        DropdownMenu(
+                            expanded = showTemplateMenu,
+                            onDismissRequest = { showTemplateMenu = false }
+                        ) {
+                            DropdownMenuItem(
+                                text = { Text("Á©∫ÁôΩÔø?(Blank)") },
+                                onClick = {
+                                    showTemplateMenu = false
+                                    onAddPage(currentPageIndex)
+                                }
+                            )
+                            DropdownMenuItem(
+                                text = { Text("Ê©´ÔøΩ?Ôø?(Lined)") },
+                                onClick = {
+                                    showTemplateMenu = false
+                                    onAddPage(currentPageIndex) // ?ÔøΩÔøΩ??ÔøΩÂÇ≥?ÔøΩÊï∏
+                                }
+                            )
+                            DropdownMenuItem(
+                                text = { Text("Á∂≤Ê†ºÔø?(Grid)") },
+                                onClick = {
+                                    showTemplateMenu = false
+                                    onAddPage(currentPageIndex) // ?ÔøΩÔøΩ??ÔøΩÂÇ≥?ÔøΩÊï∏
+                                }
+                            )
+                        }
                     }
                 }
             }
@@ -1711,6 +2004,8 @@ private fun PageThumbnail(
     strokes: List<StrokeWithPoints>,
     imageAnnotations: List<ImageAnnotationEntity>,
     textAnnotations: List<TextAnnotationEntity>,
+    modelWidthPt: Float,
+    modelHeightPt: Float,
     isSelected: Boolean,
     boxModifier: Modifier = Modifier.width(88.dp).aspectRatio(1f / 1.414f)
 ) {
@@ -1780,8 +2075,8 @@ private fun PageThumbnail(
             }
             // Unified ink + image + text overlay
             androidx.compose.foundation.Canvas(modifier = Modifier.fillMaxSize()) {
-                val modelW = 595f
-                val modelH = 842f
+                val modelW = modelWidthPt.coerceAtLeast(1f)
+                val modelH = modelHeightPt.coerceAtLeast(1f)
                 val sx = size.width / modelW
                 val sy = size.height / modelH
 
@@ -1927,7 +2222,7 @@ private fun PageIcon(pageIndex: Int, isSelected: Boolean) {
     }
 }
 
-/** ?∏ÊµÆ?®Áï´Â∏ÉÂ∑¶Á∑?∏≠Â§ÆÁ??ßÂà∂?âÈ?ÔºöÊî∂?àÂÅ¥Ê¨?+ ?®Ëû¢ÂπïÈ??¢Ê†ºÔºàÈ?ÁΩ?3 ÁßíÂ??™Â?Ê∑°Âá∫Ôº?*/
+/** ?ÔøΩÊµÆ?ÔøΩÁï´Â∏ÉÂ∑¶Ôø?ÔøΩÔøΩÂ§ÆÔøΩ??ÔøΩÂà∂?ÔøΩÔøΩ?ÔºöÊî∂?ÔøΩÂÅ¥Ôø?+ ?ÔøΩËû¢ÂπïÔøΩ??ÔøΩÊ†ºÔºàÔøΩ?Ôø?3 ÁßíÔøΩ??ÔøΩÔøΩ?Ê∑°Âá∫Ôø?*/
 @Composable
 private fun SidebarFloatingControls(
     sidebarMode: SidebarMode,
@@ -1960,7 +2255,7 @@ private fun SidebarFloatingControls(
         verticalArrangement = Arrangement.spacedBy(6.dp),
         horizontalAlignment = Alignment.CenterHorizontally
     ) {
-        // ?∂Â? / Â±ïÈ?
+        // ?ÔøΩÔøΩ? / Â±ïÔøΩ?
         androidx.compose.material3.SmallFloatingActionButton(
             onClick = {
                 activationCount++
@@ -1974,11 +2269,11 @@ private fun SidebarFloatingControls(
         ) {
             Icon(
                 imageVector = if (sidebarMode == SidebarMode.NORMAL) Icons.Default.ChevronLeft else Icons.Default.ChevronRight,
-                contentDescription = if (sidebarMode == SidebarMode.NORMAL) "?∂Â??¥Ê?" else "Â±ïÈ??¥Ê?",
+                contentDescription = if (sidebarMode == SidebarMode.NORMAL) "?ÔøΩÔøΩ??ÔøΩÔøΩ?" else "Â±ïÔøΩ??ÔøΩÔøΩ?",
                 modifier = Modifier.size(18.dp)
             )
         }
-        // ?ÅÈù¢?ºÔ??™Âú® NORMAL È°ØÁ§∫Ôº?+        // ?ÔøΩÈù¢?ÔøΩÔøΩ??ÔøΩÂú® NORMAL È°ØÁ§∫Ôø?
         androidx.compose.animation.AnimatedVisibility(
             visible = sidebarMode == SidebarMode.NORMAL,
             enter = fadeIn(tween(200)) + scaleIn(tween(200, easing = androidx.compose.animation.core.FastOutSlowInEasing), initialScale = 0.7f),
@@ -1995,7 +2290,7 @@ private fun SidebarFloatingControls(
             ) {
                 Icon(
                     Icons.Default.Apps,
-                    contentDescription = "?®Ëû¢ÂπïÈ??¢Ê†º",
+                    contentDescription = "?ÔøΩËû¢ÂπïÔøΩ??ÔøΩÊ†º",
                     modifier = Modifier.size(18.dp)
                 )
             }
@@ -2072,12 +2367,12 @@ private fun Workspace(
                 // Pan / zoom handler for the Workspace background Box.
                 //
                 // Routing contract (agreed with InkCanvas.pointerInput):
-                //   FREE          ??InkCanvas does NOT consume Touch events ??all single-finger
+                //   FREE          ??InkCanvas does NOT consume Touch events ??all single-finger
                 //                   touch contacts bubble here for pan.  Stylus is consumed by
                 //                   InkCanvas for drawing.
-                //   PALM_REJECTION ??InkCanvas does NOT consume finger-zone contacts ??single
+                //   PALM_REJECTION ??InkCanvas does NOT consume finger-zone contacts ??single
                 //                   finger pans here.  Stylus consumed for drawing.  Palm dropped.
-                //   STYLUS_ONLY   ??InkCanvas does NOT consume any Touch (finger/palm) ??all
+                //   STYLUS_ONLY   ??InkCanvas does NOT consume any Touch (finger/palm) ??all
                 //                   touch contacts bubble here for single-finger pan.
                 //
                 // For 2+ simultaneous pointers this handler handles pinch-zoom + two-finger pan
@@ -2169,7 +2464,7 @@ private fun Workspace(
             color = paperColor,
             border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant.copy(alpha = 0.32f))
         ) {
-            // PDF static layer (bottom) ??crossfade between page bitmaps
+            // PDF static layer (bottom) ??crossfade between page bitmaps
             Crossfade(
                 targetState = pageBitmap,
                 animationSpec = tween(200),
@@ -2276,17 +2571,21 @@ fun TabletEditorTopBar(
     val toolColors = listOf(Color.Black, Color(0xFF_FFC700), Color(0xFF_F44336), Color(0xFF_4CAF50))
     val shownRecentColors = recentColors.filterNot { it in toolColors }.take(8)
     var showColorPicker by remember { mutableStateOf(false) }
+    var showRecentColorPalette by rememberSaveable { mutableStateOf(false) }
     val isDarkSurface = MaterialTheme.colorScheme.background.luminance() < 0.5f
-    val shellColor = if (isDarkSurface) ToolbarGlassDark else ToolbarGlassLight
-    val clusterColor = MaterialTheme.colorScheme.surfaceContainerHigh.copy(alpha = if (isDarkSurface) 0.78f else 0.94f)
-    val borderColor = MaterialTheme.colorScheme.outlineVariant.copy(alpha = 0.58f)
-    val toolButtonSize = 36.dp
-    val utilityButtonSize = 38.dp
+    val shellColor = if (isDarkSurface) ToolbarGlassDark.copy(alpha = 0.96f) else ToolbarGlassLight.copy(alpha = 0.98f)
+    val clusterColor = MaterialTheme.colorScheme.surfaceContainerHigh.copy(alpha = if (isDarkSurface) 0.5f else 0.5f)
+    val borderColor = MaterialTheme.colorScheme.outlineVariant.copy(alpha = 0.3f)
+    val toolbarHeight = 56.dp
+    val sectionGap = 4.dp
+    val sectionPadding = 0.dp
+    val controlSize = 36.dp
+    val keyActionGradient = Brush.linearGradient(listOf(BrandIndigo, BrandPurple))
 
     val drawingTools = listOf(Tool.PEN, Tool.HIGHLIGHTER, Tool.ERASER, Tool.LASSO)
     val drawingActiveIdx = drawingTools.indexOf(activeTool).let { if (it < 0) -1 else it }
     val drawingHighlightOffset by animateDpAsState(
-        targetValue = if (drawingActiveIdx >= 0) toolButtonSize * drawingActiveIdx else 0.dp,
+        targetValue = if (drawingActiveIdx >= 0) controlSize * drawingActiveIdx else 0.dp,
         animationSpec = tween(durationMillis = 200),
         label = "DrawingToolHighlight"
     )
@@ -2294,7 +2593,7 @@ fun TabletEditorTopBar(
     val annotationTools = listOf(Tool.SHAPE, Tool.TEXT, Tool.IMAGE, Tool.STAMP)
     val annotationActiveIdx = annotationTools.indexOf(activeTool).let { if (it < 0) -1 else it }
     val annotationHighlightOffset by animateDpAsState(
-        targetValue = if (annotationActiveIdx >= 0) toolButtonSize * annotationActiveIdx else 0.dp,
+        targetValue = if (annotationActiveIdx >= 0) controlSize * annotationActiveIdx else 0.dp,
         animationSpec = tween(durationMillis = 200),
         label = "AnnotationToolHighlight"
     )
@@ -2318,60 +2617,52 @@ fun TabletEditorTopBar(
 
     Surface(
         modifier = Modifier.fillMaxWidth(),
-        color = MaterialTheme.colorScheme.surface.copy(alpha = 0.94f),
+        color = shellColor,
         contentColor = MaterialTheme.colorScheme.onSurface,
-        shadowElevation = 8.dp,
+        shadowElevation = 4.dp,
         border = BorderStroke(1.dp, borderColor)
     ) {
         Row(
             modifier = Modifier
                 .fillMaxWidth()
-                .height(68.dp)
-                .padding(horizontal = 10.dp, vertical = 6.dp),
+                .height(toolbarHeight)
+                .padding(horizontal = sectionGap, vertical = sectionPadding),
             verticalAlignment = Alignment.CenterVertically,
-            horizontalArrangement = Arrangement.spacedBy(10.dp)
+            horizontalArrangement = Arrangement.spacedBy(sectionGap)
         ) {
-            Surface(
-                shape = RoundedCornerShape(20.dp),
-                color = clusterColor,
-                border = BorderStroke(1.dp, borderColor)
+            Row(
+                modifier = Modifier
+                    .padding(start = 2.dp, end = 6.dp)
+                    .background(clusterColor, RoundedCornerShape(20.dp))
+                    .padding(horizontal = 4.dp, vertical = 2.dp),
+                verticalAlignment = Alignment.CenterVertically
             ) {
-                Row(
-                    modifier = Modifier.padding(start = 2.dp, end = 10.dp, top = 4.dp, bottom = 4.dp),
-                    verticalAlignment = Alignment.CenterVertically
-                ) {
-                    IconButton(onClick = onBack, modifier = Modifier.size(utilityButtonSize)) {
-                        Icon(Icons.AutoMirrored.Filled.ArrowBack, contentDescription = "Back to Library")
-                    }
-                    Column(modifier = Modifier.widthIn(max = 156.dp)) {
-                        Text(
-                            text = documentTitle,
-                            style = MaterialTheme.typography.titleSmall,
-                            maxLines = 1,
-                            overflow = TextOverflow.Ellipsis
-                        )
-                        Text(
-                            text = "InkFlow Studio",
-                            style = MaterialTheme.typography.labelSmall,
-                            color = MaterialTheme.colorScheme.primary
-                        )
-                    }
+                IconButton(onClick = onBack, modifier = Modifier.size(controlSize)) {
+                    Icon(Icons.AutoMirrored.Filled.ArrowBack, contentDescription = "Back to Library")
+                }
+                Column(modifier = Modifier.widthIn(max = 140.dp)) {
+                    Text(
+                        text = documentTitle,
+                        style = MaterialTheme.typography.titleSmall,
+                        maxLines = 1,
+                        overflow = TextOverflow.Ellipsis
+                    )
+                    Text(
+                        text = "InkFlow Studio",
+                        style = MaterialTheme.typography.labelSmall,
+                        color = MaterialTheme.colorScheme.primary
+                    )
                 }
             }
 
-            Surface(
-                modifier = Modifier.weight(1f),
-                shape = RoundedCornerShape(24.dp),
-                color = shellColor,
-                border = BorderStroke(1.dp, borderColor),
-                shadowElevation = 4.dp
+            Row(
+                modifier = Modifier
+                    .weight(1f)
+                    .horizontalScroll(rememberScrollState())
+                    .padding(horizontal = sectionGap, vertical = sectionPadding),
+                verticalAlignment = Alignment.CenterVertically,
+                horizontalArrangement = Arrangement.Center
             ) {
-                Row(
-                    modifier = Modifier
-                        .horizontalScroll(rememberScrollState())
-                        .padding(horizontal = 10.dp, vertical = 6.dp),
-                    verticalAlignment = Alignment.CenterVertically
-                ) {
                     Box(
                         modifier = Modifier
                             .background(clusterColor, RoundedCornerShape(20.dp))
@@ -2381,14 +2672,9 @@ fun TabletEditorTopBar(
                             Box(
                                 modifier = Modifier
                                     .offset(x = drawingHighlightOffset)
-                                    .size(toolButtonSize)
+                                    .size(controlSize)
                                     .background(
-                                        Brush.linearGradient(
-                                            listOf(
-                                                MaterialTheme.colorScheme.primaryContainer,
-                                                BrandPurple.copy(alpha = 0.82f)
-                                            )
-                                        ),
+                                        MaterialTheme.colorScheme.primaryContainer,
                                         RoundedCornerShape(16.dp)
                                     )
                             )
@@ -2464,8 +2750,8 @@ fun TabletEditorTopBar(
                                 ) {
                                     Text(
                                         text = when (subType) {
-                                            LassoSubType.FREEFORM -> "?™Áî±?àÈÅ∏"
-                                            LassoSubType.RECT -> "?©ÂΩ¢?àÈÅ∏"
+                                            LassoSubType.FREEFORM -> "?ÔøΩÁî±?ÔøΩÈÅ∏"
+                                            LassoSubType.RECT -> "?ÔøΩÂΩ¢?ÔøΩÈÅ∏"
                                         },
                                         style = MaterialTheme.typography.labelMedium
                                     )
@@ -2474,7 +2760,10 @@ fun TabletEditorTopBar(
                         }
                     }
 
-                    VerticalDivider(Modifier.height(24.dp).padding(horizontal = 8.dp))
+                    VerticalDivider(
+                        modifier = Modifier.height(24.dp).padding(horizontal = sectionGap),
+                        thickness = 1.dp
+                    )
 
                     Box(
                         modifier = Modifier
@@ -2485,14 +2774,9 @@ fun TabletEditorTopBar(
                             Box(
                                 modifier = Modifier
                                     .offset(x = annotationHighlightOffset)
-                                    .size(toolButtonSize)
+                                    .size(controlSize)
                                     .background(
-                                        Brush.linearGradient(
-                                            listOf(
-                                                MaterialTheme.colorScheme.primaryContainer,
-                                                BrandIndigo.copy(alpha = 0.78f)
-                                            )
-                                        ),
+                                        MaterialTheme.colorScheme.primaryContainer,
                                         RoundedCornerShape(16.dp)
                                     )
                             )
@@ -2562,9 +2846,9 @@ fun TabletEditorTopBar(
                                 ) {
                                     Text(
                                         text = when (subType) {
-                                            ShapeSubType.RECT -> "?πÊ?"
-                                            ShapeSubType.CIRCLE -> "?ìÂΩ¢"
-                                            ShapeSubType.LINE -> "?¥Á?"
+                                            ShapeSubType.RECT -> "?ÔøΩÔøΩ?"
+                                            ShapeSubType.CIRCLE -> "?ÔøΩÂΩ¢"
+                                            ShapeSubType.LINE -> "?ÔøΩÔøΩ?"
                                             ShapeSubType.ARROW -> "ÁÆ≠È†≠"
                                         },
                                         style = MaterialTheme.typography.labelMedium
@@ -2574,7 +2858,10 @@ fun TabletEditorTopBar(
                         }
                     }
 
-                    VerticalDivider(Modifier.height(24.dp).padding(horizontal = 8.dp))
+                    VerticalDivider(
+                        modifier = Modifier.height(24.dp).padding(horizontal = sectionGap),
+                        thickness = 1.dp
+                    )
 
                     Row(
                         modifier = Modifier
@@ -2582,17 +2869,21 @@ fun TabletEditorTopBar(
                             .padding(horizontal = 6.dp, vertical = 3.dp),
                         verticalAlignment = Alignment.CenterVertically
                     ) {
-                        shownRecentColors.forEach { color ->
+                        toolColors.forEach { color ->
                             ColorChip(color = color, isSelected = selectedColor == color) {
                                 viewModel.onColorSelected(color)
                             }
                         }
                         if (shownRecentColors.isNotEmpty()) {
-                            Spacer(Modifier.width(4.dp))
-                        }
-                        toolColors.forEach { color ->
-                            ColorChip(color = color, isSelected = selectedColor == color) {
-                                viewModel.onColorSelected(color)
+                            IconButton(
+                                onClick = { showRecentColorPalette = !showRecentColorPalette },
+                                modifier = Modifier.size(controlSize)
+                            ) {
+                                Icon(
+                                    imageVector = Icons.Default.Bookmark,
+                                    contentDescription = "Recent Colors",
+                                    tint = if (showRecentColorPalette) MaterialTheme.colorScheme.primary else MaterialTheme.colorScheme.onSurfaceVariant
+                                )
                             }
                         }
                         Box(
@@ -2618,6 +2909,26 @@ fun TabletEditorTopBar(
                         )
                     }
 
+                    AnimatedVisibility(
+                        visible = showRecentColorPalette && shownRecentColors.isNotEmpty(),
+                        enter = fadeIn(tween(160)) + androidx.compose.animation.expandHorizontally(tween(220)),
+                        exit = fadeOut(tween(120)) + androidx.compose.animation.shrinkHorizontally(tween(160))
+                    ) {
+                        Row(
+                            modifier = Modifier
+                                .padding(start = sectionGap)
+                                .background(clusterColor, CircleShape)
+                                .padding(horizontal = 6.dp, vertical = 3.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            shownRecentColors.forEach { color ->
+                                ColorChip(color = color, isSelected = selectedColor == color) {
+                                    viewModel.onColorSelected(color)
+                                }
+                            }
+                        }
+                    }
+
                     AnimatedVisibility(
                         visible = showStrokeWidthSlider && (activeTool == Tool.PEN || activeTool == Tool.HIGHLIGHTER),
                         enter = fadeIn(tween(180)) + androidx.compose.animation.expandHorizontally(tween(220)),
@@ -2639,20 +2950,17 @@ fun TabletEditorTopBar(
                 }
             }
 
-            Surface(
-                shape = RoundedCornerShape(20.dp),
-                color = shellColor,
-                border = BorderStroke(1.dp, borderColor)
+            Row(
+                modifier = Modifier
+                    .background(clusterColor, RoundedCornerShape(20.dp))
+                    .padding(horizontal = 4.dp, vertical = 2.dp),
+                verticalAlignment = Alignment.CenterVertically
             ) {
-                Row(
-                    modifier = Modifier.padding(horizontal = 6.dp, vertical = 3.dp),
-                    verticalAlignment = Alignment.CenterVertically
-                ) {
-                    IconButton(
-                        onClick = { viewModel.undo() },
+                IconButton(
+                    onClick = { viewModel.undo() },
                         enabled = canUndo,
                         modifier = Modifier
-                            .size(utilityButtonSize)
+                            .size(controlSize)
                             .graphicsLayer { scaleX = undoScale; scaleY = undoScale }
                     ) {
                         Icon(Icons.AutoMirrored.Filled.Undo, contentDescription = "Undo")
@@ -2661,33 +2969,40 @@ fun TabletEditorTopBar(
                         onClick = { viewModel.redo() },
                         enabled = canRedo,
                         modifier = Modifier
-                            .size(utilityButtonSize)
+                            .size(controlSize)
                             .graphicsLayer { scaleX = redoScale; scaleY = redoScale }
                     ) {
                         Icon(Icons.AutoMirrored.Filled.Redo, contentDescription = "Redo")
                     }
                     Box(
                         modifier = Modifier
-                            .padding(horizontal = 4.dp)
-                            .clip(CircleShape)
-                            .background(MaterialTheme.colorScheme.primaryContainer)
+                            .padding(horizontal = 3.dp)
+                            .clip(RoundedCornerShape(12.dp))
+                            .background(keyActionGradient)
                     ) {
-                        IconButton(onClick = onExport, modifier = Modifier.size(utilityButtonSize)) {
+                        IconButton(onClick = onExport, modifier = Modifier.size(controlSize)) {
                             Icon(
                                 Icons.Outlined.FileUpload,
                                 contentDescription = "Export PDF",
-                                tint = MaterialTheme.colorScheme.onPrimaryContainer
+                                tint = Color.White
                             )
                         }
                     }
-                    IconButton(onClick = onDocumentSettings, modifier = Modifier.size(utilityButtonSize)) {
-                        Icon(
-                            imageVector = Icons.AutoMirrored.Filled.Article,
-                            contentDescription = "Document Settings",
-                            tint = MaterialTheme.colorScheme.onSurface
-                        )
+                    Box(
+                        modifier = Modifier
+                            .padding(horizontal = 3.dp)
+                            .clip(RoundedCornerShape(12.dp))
+                            .background(keyActionGradient)
+                    ) {
+                        IconButton(onClick = onDocumentSettings, modifier = Modifier.size(controlSize)) {
+                            Icon(
+                                imageVector = Icons.AutoMirrored.Filled.Article,
+                                contentDescription = "Document Settings",
+                                tint = Color.White
+                            )
+                        }
                     }
-                    IconButton(onClick = { viewModel.cycleInputMode() }, modifier = Modifier.size(utilityButtonSize)) {
+                    IconButton(onClick = { viewModel.cycleInputMode() }, modifier = Modifier.size(controlSize)) {
                         Icon(
                             imageVector = when (inputMode) {
                                 InputMode.FREE -> Icons.Filled.TouchApp
@@ -2717,21 +3032,24 @@ private fun DocumentSettingsDialog(
     documentTitle: String,
     pageCount: Int,
     currentPageIndex: Int,
+    currentViewMode: PageViewMode,
     currentStyle: PaperStyle,
     isPageOperationInProgress: Boolean,
     onDismiss: () -> Unit,
     onConfirmStyle: (PaperStyle) -> Unit,
+    onConfirmViewMode: (PageViewMode) -> Unit,
     onInsertPdf: () -> Unit
 ) {
     var selectedBackground by remember { mutableStateOf(currentStyle.background) }
+    var selectedViewMode by remember { mutableStateOf(currentViewMode) }
 
     androidx.compose.material3.AlertDialog(
         onDismissRequest = onDismiss,
-        title = { Text("?á‰ª∂Ë®≠Â?‰∏≠Â?", style = MaterialTheme.typography.titleLarge) },
+        title = { Text("?ÔøΩ‰ª∂Ë®≠ÔøΩ?‰∏≠ÔøΩ?", style = MaterialTheme.typography.titleLarge) },
         text = {
             Column(verticalArrangement = Arrangement.spacedBy(12.dp)) {
                 Text(
-                    text = "?®ÈÄôË£°Ë™øÊï¥?ÆÂ??á‰ª∂?ÑÈ°ØÁ§∫Ë®≠ÂÆöÔ??ñÊ??¶‰?‰ª?PDF ?íÂÖ•?ÆÂ??ÅÂ??π„Ä?,
+                    text = "Please select", 
                     style = MaterialTheme.typography.bodySmall,
                     color = MaterialTheme.colorScheme.onSurfaceVariant
                 )
@@ -2746,7 +3064,7 @@ private fun DocumentSettingsDialog(
                             .padding(16.dp),
                         verticalArrangement = Arrangement.spacedBy(4.dp)
                     ) {
-                        Text("?á‰ª∂Ë≥áË?", style = MaterialTheme.typography.titleSmall)
+                        Text("?ÔøΩ‰ª∂Ë≥áÔøΩ?", style = MaterialTheme.typography.titleSmall)
                         Text(
                             text = documentTitle,
                             style = MaterialTheme.typography.bodyMedium,
@@ -2754,7 +3072,7 @@ private fun DocumentSettingsDialog(
                             overflow = TextOverflow.Ellipsis
                         )
                         Text(
-                            text = "??$pageCount ?ÅÔ??ÆÂ?Á¨?${currentPageIndex + 1} ??,
+                    text = "Please select", 
                             style = MaterialTheme.typography.bodySmall,
                             color = MaterialTheme.colorScheme.onSurfaceVariant
                         )
@@ -2762,17 +3080,42 @@ private fun DocumentSettingsDialog(
                 }
 
                 Text(
-                    text = "?ÅÈù¢?åÊôØÊ®??",
+                    text = "Please select", 
+                    style = MaterialTheme.typography.bodySmall,
+                    color = MaterialTheme.colorScheme.onSurfaceVariant
+                )
+
+                Row(
+                    modifier = Modifier.horizontalScroll(rememberScrollState()),
+                    horizontalArrangement = Arrangement.spacedBy(8.dp)
+                ) {
+                    listOf(
+                        PageViewMode.SINGLE_PAGE to "?ÔøΩÔøΩ?Á∑®ËºØ",
+                        PageViewMode.CONTINUOUS_SCROLL to "????ÔøΩÔøΩ?"
+                    ).forEach { (mode, label) ->
+                        FilterChip(
+                            selected = selectedViewMode == mode,
+                            onClick = { selectedViewMode = mode },
+                            label = { Text(label) },
+                            leadingIcon = if (selectedViewMode == mode) {
+                                { Icon(Icons.Default.Check, contentDescription = null, modifier = Modifier.size(16.dp)) }
+                            } else null
+                        )
+                    }
+                }
+
+                Text(
+                    text = "Please select", 
                     style = MaterialTheme.typography.bodySmall,
                     color = MaterialTheme.colorScheme.onSurfaceVariant
                 )
 
                 val bgOptions = listOf(
                     PageBackground.BLANK to "Á©∫ÁôΩ",
-                    PageBackground.RULED to "Ê©´Á?",
-                    PageBackground.NARROW_RULED to "ÂØÜË?",
-                    PageBackground.WIDE_RULED to "ÂØ¨Ë?",
-                    PageBackground.GRID to "?πÊ†º",
+                    PageBackground.RULED to "Ê©´ÔøΩ?",
+                    PageBackground.NARROW_RULED to "ÂØÜÔøΩ?",
+                    PageBackground.WIDE_RULED to "ÂØ¨ÔøΩ?",
+                    PageBackground.GRID to "?ÔøΩÊ†º",
                     PageBackground.DOT_GRID to "ÈªûÊ†º",
                 )
 
@@ -2807,12 +3150,12 @@ private fun DocumentSettingsDialog(
                             modifier = Modifier.weight(1f),
                             verticalArrangement = Arrangement.spacedBy(4.dp)
                         ) {
-                            Text("?íÂÖ• PDF", style = MaterialTheme.typography.titleSmall)
+                            Text("?ÔøΩÂÖ• PDF", style = MaterialTheme.typography.titleSmall)
                             Text(
                                 text = if (isPageOperationInProgress) {
-                                    "Ê≠?ú®?¥Êñ∞?ÅÈù¢ÔºåÂ??êÂ??´Ê??°Ê??çÊ??•„Ä?
+                                    "Ôø?ÔøΩÔøΩ?ÔøΩÊñ∞?ÔøΩÈù¢ÔºåÔøΩ??ÔøΩÔøΩ??ÔøΩÔøΩ??ÔøΩÔøΩ??ÔøΩÔøΩ??ÔøΩÔøΩ?
                                 } else {
-                                    "ÂæûÊ?Ê°àÊ???PDFÔºåÊ??•Âà∞?ÆÂ?Á¨?${currentPageIndex + 1} ?ÅÂ??π„Ä?
+                                    "ÂæûÔøΩ?Ê°àÔøΩ???PDFÔºåÔøΩ??ÔøΩÂà∞?ÔøΩÔøΩ?Ôø?${currentPageIndex + 1} ?ÔøΩÔøΩ??ÔøΩÔøΩ?
                                 },
                                 style = MaterialTheme.typography.bodySmall,
                                 color = MaterialTheme.colorScheme.onSurfaceVariant
@@ -2825,7 +3168,7 @@ private fun DocumentSettingsDialog(
                                 contentColor = MaterialTheme.colorScheme.primary
                             )
                         ) {
-                            Text(if (isPageOperationInProgress) "?ØÂÖ•‰∏? else "?∏Ê? PDF")
+                            Text(if (isPageOperationInProgress) "?ÔøΩÂÖ•Ôø? else "?ÔøΩÔøΩ? PDF")
                         }
                     }
                 }
@@ -2834,11 +3177,12 @@ private fun DocumentSettingsDialog(
         confirmButton = {
             androidx.compose.material3.TextButton(onClick = {
                 onConfirmStyle(currentStyle.copy(background = selectedBackground))
+                onConfirmViewMode(selectedViewMode)
                 onDismiss()
-            }) { Text("Á¢∫Ë?") }
+            }) { Text("Á¢∫ÔøΩ?") }
         },
         dismissButton = {
-            androidx.compose.material3.TextButton(onClick = onDismiss) { Text("?ñÊ?") }
+            androidx.compose.material3.TextButton(onClick = onDismiss) { Text("?ÔøΩÔøΩ?") }
         }
     )
 }
@@ -2863,11 +3207,11 @@ private fun NewDocPaperSizeDialog(
 
     androidx.compose.material3.AlertDialog(
         onDismissRequest = onDismiss,
-        title = { Text("?∏Ê?Á¥ôÂºµÂ§ßÂ?", style = MaterialTheme.typography.titleLarge) },
+        title = { Text("?ÔøΩÔøΩ?Á¥ôÂºµÂ§ßÔøΩ?", style = MaterialTheme.typography.titleLarge) },
         text = {
             Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
                 Text(
-                    text = "Ë®≠Â??∞Á©∫?ΩÁ?Ë®òÁ?Á¥ôÂºµÂ§ßÂ?ÔºàÂª∫Á´ãÂ??°Ê??¥ÊîπÔº?,
+                    text = "Please select", 
                     style = MaterialTheme.typography.bodySmall,
                     color = MaterialTheme.colorScheme.onSurfaceVariant,
                     modifier = Modifier.padding(bottom = 8.dp)
@@ -2879,7 +3223,7 @@ private fun NewDocPaperSizeDialog(
                         FilterChip(
                             selected = isPortrait,
                             onClick = { selectedWidth = w; selectedHeight = h },
-                            label = { Text("$label ?¥Â?") },
+                            label = { Text("$label ?ÔøΩÔøΩ?") },
                             leadingIcon = if (isPortrait) {
                                 { Icon(Icons.Default.Check, contentDescription = null, modifier = Modifier.size(16.dp)) }
                             } else null
@@ -2887,7 +3231,7 @@ private fun NewDocPaperSizeDialog(
                         FilterChip(
                             selected = isLandscape,
                             onClick = { selectedWidth = h; selectedHeight = w },
-                            label = { Text("$label Ê©´Â?") },
+                            label = { Text("$label Ê©´ÔøΩ?") },
                             leadingIcon = if (isLandscape) {
                                 { Icon(Icons.Default.Check, contentDescription = null, modifier = Modifier.size(16.dp)) }
                             } else null
@@ -2899,15 +3243,15 @@ private fun NewDocPaperSizeDialog(
         confirmButton = {
             androidx.compose.material3.TextButton(onClick = {
                 onCreate(selectedWidth, selectedHeight)
-            }) { Text("Âª∫Á?") }
+            }) { Text("Âª∫ÔøΩ?") }
         },
         dismissButton = {
-            androidx.compose.material3.TextButton(onClick = onDismiss) { Text("?ñÊ?") }
+            androidx.compose.material3.TextButton(onClick = onDismiss) { Text("?ÔøΩÔøΩ?") }
         }
     )
 }
 
-// ?∞Â?: È°èËâ≤?∏Ê???Chip ?É‰ª∂ÔºåÂ∏∂?âÈÅ∏?áÁ???+// ?ÔøΩÔøΩ?: È°èËâ≤?ÔøΩÔøΩ???Chip ?ÔøΩ‰ª∂ÔºåÂ∏∂?ÔøΩÈÅ∏?ÔøΩÔøΩ???
 @Composable
 private fun ColorChip(color: Color, isSelected: Boolean, onClick: () -> Unit) {
     val animatedBorder by animateDpAsState(if (isSelected) 2.dp else 1.dp, label = "ColorChipBorder")
@@ -3022,18 +3366,38 @@ private fun EditorIconButton(
 
 /** Instantly scroll so [index] is vertically centered in the sidebar. */
 private suspend fun LazyListState.scrollToCenter(index: Int) {
-    scrollToItem(index)
-    val itemInfo = layoutInfo.visibleItemsInfo.firstOrNull { it.index == index } ?: return
     val viewportHeight = layoutInfo.viewportEndOffset - layoutInfo.viewportStartOffset
-    val delta = (itemInfo.offset + itemInfo.size / 2 - viewportHeight / 2).toFloat()
-    scroll { scrollBy(delta) }
+    var itemInfo = layoutInfo.visibleItemsInfo.firstOrNull { it.index == index }
+    
+    if (itemInfo == null) {
+        scrollToItem(index)
+        itemInfo = layoutInfo.visibleItemsInfo.firstOrNull { it.index == index }
+    }
+    
+    if (itemInfo != null) {
+        val centerOffset = (viewportHeight - itemInfo.size) / 2
+        scrollToItem(index, scrollOffset = -centerOffset)
+    }
 }
 
 /** Animated scroll so [index] is vertically centered in the sidebar. */
 private suspend fun LazyListState.animateScrollToCenter(index: Int) {
-    scrollToItem(index)
-    val itemInfo = layoutInfo.visibleItemsInfo.firstOrNull { it.index == index } ?: return
     val viewportHeight = layoutInfo.viewportEndOffset - layoutInfo.viewportStartOffset
-    val delta = (itemInfo.offset + itemInfo.size / 2 - viewportHeight / 2).toFloat()
-    animateScrollBy(delta)
+    var itemInfo = layoutInfo.visibleItemsInfo.firstOrNull { it.index == index }
+    
+    if (itemInfo != null) {
+        val centerOffset = (viewportHeight - itemInfo.size) / 2
+        animateScrollToItem(index, scrollOffset = -centerOffset)
+    } else {
+        // ?ÔøΩË∑≥ËΩâÂà∞Ë©≤ÔøΩ??ÔøΩÔøΩ?ÁΩÆÔøΩ?‰ª•‰æøË®àÔøΩ?È´òÂ∫¶
+        scrollToItem(index)
+        // Ë∑≥ÔøΩ?ÂæåÔøΩ?Ë¶ÅÔøΩ?ÂæóÊñ∞?ÔøΩÔøΩ?Ôø?
+        itemInfo = layoutInfo.visibleItemsInfo.firstOrNull { it.index == index }
+        if (itemInfo != null) {
+            val centerOffset = (viewportHeight - itemInfo.size) / 2
+            // ‰ΩøÁî®?ÔøΩÔøΩ??ÔøΩÔøΩ??ÔøΩÔøΩ?Ê≠•ÔøΩ??ÔøΩ‰øÆÔø?
+            animateScrollToItem(index, scrollOffset = -centerOffset)
+        }
+    }
 }
+

